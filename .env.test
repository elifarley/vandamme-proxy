# Test environment configuration for Vandamme Proxy
# This file provides mock configuration for running tests

# Mock API keys for testing (not real keys)
OPENAI_API_KEY="test-openai-key-for-testing"
ANTHROPIC_API_KEY="test-anthropic-key-for-testing"

# Test provider configuration
VDM_DEFAULT_PROVIDER="openai"

# Use standard endpoints (these won't be actually called in unit tests)
OPENAI_BASE_URL="https://api.openai.com/v1"

# Test server settings
HOST="0.0.0.0"
PORT="8082"
LOG_LEVEL="DEBUG"  # More verbose for testing

# Performance settings optimized for testing
MAX_TOKENS_LIMIT="4096"
MIN_TOKENS_LIMIT="100"
REQUEST_TIMEOUT="30"  # Shorter timeout for tests
MAX_RETRIES="1"  # Fewer retries for faster tests